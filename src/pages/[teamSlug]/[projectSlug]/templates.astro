---
import { PlateContainer } from '@/containers/templates/plate-container'
import ProjectLayout from '@/layouts/project/project-layout.astro'

const plateNodeId = Math.random().toString(36).slice(2, 7)
---

<ProjectLayout>
  <PlateContainer plateNodeId={plateNodeId} client:load />
</ProjectLayout>
